name: CI
on:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Intentional fail (issue2pr 재작업 루프 검증용 — 검증 후 삭제)
        run: |
          echo "Intentionally failing to exercise the issue2pr rework loop."
          exit 1
